<html>
    <head>
        <meta charset="UTF-8">
        <title>Document</title>
    </head>
    <body>
         <form action="{{ path('add_city') }}" method="post">
                <label for="Nom de la ville">Nom de la ville</label>
                <input type="text" name="cityname" id="nom" placeholder="Nom de la ville" class="form-control">

                <button type="submit" class="btn btn-primary">Ajouter</button>
         </form>
    </body>
</html>